# pnpm 配置：使用 npm 风格扁平 node_modules
#
# 为什么要这样：app/ 是 React Native + Expo，metro bundler 用 Node 风格的解析，
# 必须从 <pkg>/node_modules 直接找到所有传递依赖；pnpm 严格模式（默认）只把
# 直接依赖放到 <pkg>/node_modules，会导致 'Unable to resolve "expo-modules-core"'
# 这类报错（每个 RN 包内部都 import 一堆兄弟包）。
#
# node-linker=hoisted 让整个 workspace 用扁平 node_modules，对 backend/frontend/shared
# 完全兼容，对 app 是必须。
node-linker=hoisted

# 待做清单

> 2026-06-08 整理，来自全项目代码审查的剩余优化建议。已完成项已划线。

---

## ~~1. 统一 shared 包的构建与引用方式~~ ✅ 已完成

~~三端引用不一致（backend/frontend 走 src，miniapp 走 dist）。已通过在 `.gitignore` 中阻止 `shared/src/**/*.js` 入 git 缓解，但核心问题仍建议统一。~~

---

## 2. API Client 公共逻辑抽到 shared

**现状**：三端各自实现了一套 token 刷新 + 401 重试 + 错误格式化的逻辑：

- `miniapp/src/api/client.ts`
- `frontend/src/api/client.ts`
- `backend`（服务端无 client）

**建议**：把 transport-agnostic 部分（token 生命周期管理、错误码映射、401 重试策略）抽到 `shared/api/client.ts`，各端只注入具体的请求函数（`fetch` / `Taro.request` / `axios`）。

---

## 3. auth.store 命名重构：useAuth → authStore + useAuthStore

**现状**：`useAuth` 既被用于 React hook 订阅，又被大量以 `useAuth.getState()/setState()` 方式绕过 React 调用（如 `task.manager.ts`、`client.ts`）。

**建议**：拆分双形态：
```ts
export const authStore = create<AuthState>(...);
export const useAuthStore = authStore;
```

---

## 4. 图片分片上传 + 断点续传

**现状**：每张图一个完整 HTTP 请求（`Taro.uploadFile`），暂停后恢复从图开头重传，无失败自动重试。
`wx.uploadFile` 本身不支持分片。

**方案设想**：
- 前端：`wx.getFileSystemManager().readFile` → 手动切片 → 逐 chunk `wx.request` 发送
- 后端：`POST /:shareId/photos/:uploadId/chunk` + 最终合并接口
- chunk-track 表记录每个上传进度，支持中断后从最后一个成功 chunk 续传
- 失败 chunk 自动重试（最多 3 次）

**预估改动量**：大

---

## 总结

| # | 项 | 预估改动量 | 优先级 |
|---|---|---|---|
| 2 | API Client 公共逻辑 | 大 | 低 |
| 3 | auth.store 重命名 | 小 | 低 |
| 4 | 分片上传 + 断点续传 | 大 | 中 |

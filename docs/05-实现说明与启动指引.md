# 在线共享相册 — MVP 实现说明

> 本文档记录 MVP 阶段实际落地的代码结构、启动方式与扩展指引。
> 与代码同步维护；架构原则参见 `03-系统架构.md`，API 契约参见 `04-数据模型与接口.md`。

---

## 1. 目录结构（实际版）

```
photo/
├── docs/                                # 项目文档
├── shared/                              # 前后端共享类型 / 常量 / 校验
│   └── src/
│       ├── constants/                   # share-code, limits, error-codes
│       ├── dto/                         # auth.ts, share.ts, ApiResponse
│       ├── schema/                      # Zod schema (auth, share)
│       └── utils/time.ts
├── backend/                             # Fastify 5 + Drizzle + better-sqlite3
│   ├── drizzle.config.ts
│   ├── .env / .env.example
│   └── src/
│       ├── server.ts                    # 启动入口（自动跑迁移 + 优雅关闭）
│       ├── app.ts                       # Fastify 实例装配
│       ├── config/index.ts              # 环境变量解析
│       ├── common/                      # errors / id / time
│       ├── db/
│       │   ├── schema.ts                # users / refresh_tokens / shares / photos
│       │   ├── client.ts                # better-sqlite3 + drizzle 实例
│       │   ├── migrate.ts               # 启动时 migrator 调用
│       │   ├── cli-migrate.ts           # `pnpm db:push` 的入口
│       │   └── migrations/              # drizzle-kit 生成的 SQL
│       ├── infra/
│       │   ├── storage/                 # paths.ts / local.ts（创建/删除目录）
│       │   ├── image/processor.ts       # sharp 生成 thumb / medium
│       │   ├── auth/                    # password (argon2) / tokens (sha256)
│       │   └── archive/zip.ts           # archiver 流式 zip
│       ├── plugins/
│       │   ├── auth.plugin.ts           # @fastify/jwt + authenticate 装饰器
│       │   └── error-handler.plugin.ts  # BizError / Zod / 兜底 500
│       └── modules/
│           ├── auth/                    # register / login / refresh / logout / me
│           ├── share/                   # CRUD + 续期 + 结束
│           ├── photo/                   # 上传 / 删除
│           ├── viewer/                  # 凭码访问、缩略图、原图、zip
│           └── cleanup/cleanup.cron.ts  # node-cron 每分钟扫
├── frontend/                            # Vue 3 + TS + TDesign + UnoCSS
│   ├── vite.config.ts                   # 含 /api 代理到 :3000
│   ├── uno.config.ts
│   └── src/
│       ├── main.ts / App.vue / env.d.ts
│       ├── router/index.ts              # 含登录守卫
│       ├── stores/auth.store.ts         # Pinia：用户态 + tokenStore 集成
│       ├── api/
│       │   ├── client.ts                # fetch 封装：token、自动 refresh、XHR 上传带进度
│       │   ├── auth.api.ts
│       │   └── share.api.ts             # 含 share / photo 接口与图片 URL 构造
│       ├── pages/
│       │   ├── home/HomePage.vue        # 首页 + 凭码入口
│       │   ├── auth/                    # LoginPage / RegisterPage
│       │   ├── me/
│       │   │   ├── MySharesPage.vue     # 列表 + 续期 + 结束 + 倒计时
│       │   │   └── NewSharePage.vue     # 上传：拖拽 + 并发 + 进度 + 压缩开关
│       │   ├── viewer/ViewerPage.vue    # 凭码网格 + PhotoSwipe + zip 下载
│       │   └── NotFoundPage.vue
│       └── styles/global.css
├── package.json + pnpm-workspace.yaml
└── README.md
```

运行时生成（已 gitignore）：

```
data/app.db                              # SQLite
storage/originals/<shareId>/<photoId>.<ext>
storage/previews/<shareId>/<photoId>.jpg
storage/mediums/<shareId>/<photoId>.jpg
.npm-cache/                              # 项目级 npm 缓存（绕过 ~/.npm 权限问题）
```

---

## 2. 启动与运行

```bash
# 首次安装（注意 npm 缓存路径，详见踩坑节）
npm_config_cache="$PWD/.npm-cache" pnpm install

# 准备后端 .env（默认值能直接跑）
cp backend/.env.example backend/.env

# 初始化或更新数据库
pnpm db:push

# 同时启动前后端
pnpm dev
```

- 前端：http://localhost:5173
- 后端：http://localhost:3000（Vite 代理 `/api` 到此）
- 健康检查：http://localhost:3000/api/health

`pnpm dev` 内部并行执行：

| 命令 | 说明 |
| --- | --- |
| `pnpm dev:backend` | `tsx watch --env-file=.env src/server.ts` |
| `pnpm dev:frontend` | `vite` |

---

## 3. 关键实现要点

### 3.1 JWT 双 Token + Cookie

- Access token：`@fastify/jwt`，有效期 15min，前端存内存（Pinia）
- Refresh token：自定义随机串（`<jti>.<base64url>`），SHA-256 哈希存 DB；下发到 `httpOnly + SameSite=Lax` cookie，path `/api/auth`
- Rotate：每次 `/refresh` 签新 token + 吊销旧 token
- 重放检测：被吊销的 refresh 再次使用 → 吊销该用户全部 refresh（强制下线）
- 前端 `client.ts` 在 401 + 自动 refresh 一次后重试原请求；并发请求共用同一个刷新 Promise

### 3.2 上传链路

1. 前端选图 → 选 TTL → 选「原图 / 压缩」
2. `POST /api/shares` 拿到 shareId / shareCode
3. 文件并发上传（限 3 路）：
   - `compressed` 模式：`browser-image-compression`（长边 4000，JPEG q0.92）
   - `original` 模式：直接走 multipart
4. 后端 `@fastify/multipart` 流式落盘到 `storage/originals/<shareId>/<photoId>.<ext>`
5. sharp 同步生成 thumb / medium，写入 photos 表
6. 任一步失败：清理该文件、给出错误码

### 3.3 查看链路

- `GET /api/v/:code` → 校验 `active` + 未过期 + 未结束 → 返回照片元
- 网格用 `<img loading="lazy" src="/api/v/:code/photos/:id/thumb">`
- 大图：PhotoSwipe 直接打开 original URL（标准做法）；如果未来要先 medium 后 original，把 PhotoSwipe 的 `data-pswp-src` 换成 medium，自定义 UI 加「查看/下载原图」即可
- 全量 zip：`archiver({ store: true })` pipe 到 `reply.raw`，文件名冲突时追加 `<photoId 前 6 位>` 后缀

### 3.4 过期清理

- `node-cron` 每分钟扫一次 `cleanup.runCleanupOnce()`
- 启动时立即跑一次，避免重启窗口期残留
- 顺序：删文件目录 → 删 photos 行 → 更新 share.status='cleaned'
- 单轮上限 50 个 share，避免长尾任务卡死
- 任一 share 失败仅记 error 日志，不抛出影响整体调度

---

## 4. 数据流与错误处理

### 4.1 响应格式

成功：`{ data: <T> }` ｜ 失败：`{ error: { code, message, details? } }`

### 4.2 错误码常量

定义在 `shared/src/constants/error-codes.ts`，前后端共用（可由前端针对性 UI 提示）。

### 4.3 错误流转

- 业务错误：抛 `BizError(code, message, statusCode)` → 错误处理插件转 `{error: ...}`
- Zod 校验错误：自动转 `VALIDATION_FAILED`
- 上传过大：`@fastify/multipart` 抛 `FST_REQ_FILE_TOO_LARGE` → 转 `FILE_TOO_LARGE`
- 限流：429 → `RATE_LIMITED`
- 兜底：未识别异常 → 500 + `INTERNAL_ERROR`，原始堆栈仅写日志

---

## 5. 测试与验证

### 5.1 已通过的端到端冒烟（手工 curl）

| 场景 | 接口 | 结果 |
| --- | --- | --- |
| 健康检查 | `GET /api/health` | ✅ 200 |
| 注册 | `POST /api/auth/register` | ✅ 返回 user + access token + refresh cookie |
| 登录 | `POST /api/auth/login` | ✅ |
| 创建分享 | `POST /api/shares` | ✅ 返回 8 位分享码 |
| 上传图片 | `POST /api/shares/:id/photos`（multipart） | ✅ 自动生成 thumb/medium |
| 凭码访问 | `GET /api/v/:code` | ✅ 返回相册元数据 |
| 拉缩略图 | `GET /api/v/:code/photos/:pid/thumb` | ✅ JPEG |
| zip 下载 | `GET /api/v/:code/download` | ✅ 标准 zip 包含原图 |
| cron | 启动日志 + 每分钟扫描 | ✅ |

### 5.2 待补的测试

- 单元测试（Vitest）：service 层关键分支（refresh rotate / 续期边界 / 清理顺序）
- 集成测试：使用 Fastify inject API 跑完整请求路径
- 前端 e2e（可选）：Playwright 跑核心用户旅程

---

## 6. 踩坑记录（部署/重装环境时参考）

### 6.1 better-sqlite3 prebuild 与 Node ABI

- 项目锁 `better-sqlite3@11.5.0`，darwin-arm64 同时含 Node v115/v127/v131 的 prebuild（Node 20/22/24 都覆盖）。
- **切换 Node 版本后必须重装 native 模块**，否则会报：
  ```
  NODE_MODULE_VERSION 115 ≠ 127  (Node 20 装的二进制 vs Node 22 运行)
  ```
  解决：
  ```bash
  rm -rf node_modules/.pnpm/better-sqlite3* node_modules/better-sqlite3 backend/node_modules/better-sqlite3
  npm_config_cache="$PWD/.npm-cache" pnpm install
  ```

### 6.2 ~/.npm/_prebuilds/ 权限

部分 macOS 上该目录被 root 占用，导致 `prebuild-install` 写入失败回退到 native 编译，再因为只有 CLT 没有完整 Xcode 而失败。

固定方案：使用项目级缓存

```bash
npm_config_cache="$PWD/.npm-cache" pnpm install
```

### 6.3 drizzle-kit push 是交互式

不便自动化。本项目用 `drizzle-kit generate` 生成迁移，结合 `drizzle-orm/better-sqlite3/migrator` 在启动时和 `pnpm db:push` 命令中应用。修改 schema 流程：

```bash
# 1. 改 backend/src/db/schema.ts
# 2. 生成迁移
cd backend && pnpm db:generate
# 3. 应用迁移（或直接重启后端，server.ts 启动时也会自动应用）
pnpm db:push
```

### 6.4 Vue 模板里 inline arrow function 不能写 TS 类型

Vue 模板 SFC 里 `@click="(d: { value: number }) => ..."` 会被 vue-tsc 当 JS 解析报错。
解决：把回调抽到 `<script setup>` 里命名函数，模板里只写 `@click="onClick"`。

---

## 7. 扩展指引

### 7.1 增加一张表 / 一个字段

1. 改 `backend/src/db/schema.ts`
2. `cd backend && pnpm db:generate` 生成迁移
3. 重启后端（`server.ts` 自动应用）

### 7.2 增加一个新的 API 模块

```
backend/src/modules/<feat>/
  <feat>.routes.ts   # 路由声明
  <feat>.service.ts  # 业务逻辑
  (<feat>.repository.ts 如有需要)
```

在 `app.ts` 注册：`await app.register(featRoutes, { prefix: '/api/<feat>' })`

### 7.3 前端新增一个页面

1. `frontend/src/pages/<feat>/<Page>.vue`
2. `frontend/src/router/index.ts` 注册路由（按需加 `meta.requiresAuth`）

### 7.4 共享类型

放到 `shared/src/dto/` 或 `shared/src/constants/`，导出后前后端按 `@photo/shared` 引用。

---

## 8. 后续路线

| 优先级 | 任务 | 备注 |
| --- | --- | --- |
| 中 | 单元 + 集成测试 | 至少 service 层关键路径 |
| 中 | UI 打磨 | 移动端适配、空态、loading 骨架 |
| 中 | 优化前端首屏 chunk | TDesign 按需引入或 manualChunks |
| 低 | Docker 化 | 配套部署文档 |
| 待启动 | AI 筛图（剔除模糊/拍废） | 模型选型与离线/在线推理方案待定 |
| 待启动 | AI 选片 | 高光识别 |
| 长期 | 对象存储驱动（S3/OSS/COS） | `infra/storage` 已抽象接口，新增驱动即可 |
| 长期 | HEIC 服务端转码 | sharp 编译时启用 libheif |
